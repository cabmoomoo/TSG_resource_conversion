-- All mods must have a manifest.lua file in their root folder, with the
-- following structure.
return {
    name = "Resource Conversion",
    description = "Make use of new buildings to convert base resources into more advanced ones, some of which can't be harvested naturally.",
    -- This field is mandatory and for now it has to be 4.
    api_version = 4
}
